# Lua Comment

Go to anywhere in the application, and work on comment out sections.

## Shortcuts
- `gcc` : Toggle comment on the current line
- `gc`  : Toggle comment on the selected lines (visual mode)
- `gc{motion}` : Toggle comment based on motion (e.g., `gciw`)
